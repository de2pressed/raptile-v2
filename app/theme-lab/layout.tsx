import type { ReactNode } from "react";

export const metadata = {
  title: {
    absolute: "Theme Lab | Raptile Studio",
  },
};

export default function ThemeLabLayout({ children }: { children: ReactNode }) {
  return children;
}
